import React from 'react'

const Login = () =>{
    const login = useSelector((state)=> state.login.value);
    return (
        <div>
            <Login/>
        </div>
    )
}

export default Login